// A new data type to get the values from the brew in the database

class Brew {
  final String name;
  final String sugars;
  final int strength;

  Brew( {this.name, this.sugars, this.strength});
} 